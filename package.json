{
  "name": "3d-moon",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "esbuild": "esbuild main.js --bundle --watch --sourcemap --minify --outfile=public/main.js"
  },
  "devDependencies": {
    "esbuild": "^0.17.7",
    "events": "^3.3.0",
    "three": "^0.149.0",
    "vite": "^4.1.0"
  }
}
